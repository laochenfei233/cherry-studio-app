///
/// HybridCherryBackgroundPressViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridCherryBackgroundPressViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridCherryBackgroundPressViewSpecSwift.hpp"
#import "CherryStudioUI-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::cherrystudio::ui;
using namespace margelo::nitro::cherrystudio::ui::views;

/**
 * Represents the React Native View holder for the Nitro "CherryBackgroundPressView" HybridView.
 */
@interface HybridCherryBackgroundPressViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridCherryBackgroundPressViewComponent {
  std::shared_ptr<HybridCherryBackgroundPressViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridCherryBackgroundPressViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridCherryBackgroundPressViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridCherryBackgroundPressViewSpec> hybridView = CherryStudioUI::CherryStudioUIAutolinking::createCherryBackgroundPressView();
    _hybridView = std::dynamic_pointer_cast<HybridCherryBackgroundPressViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  CherryStudioUI::HybridCherryBackgroundPressViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridCherryBackgroundPressViewProps const>(props);
  auto& newViewProps = const_cast<HybridCherryBackgroundPressViewProps&>(newViewPropsConst);
  CherryStudioUI::HybridCherryBackgroundPressViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // enabled: boolean
  if (newViewProps.enabled.isDirty) {
    swiftPart.setEnabled(newViewProps.enabled.value);
    newViewProps.enabled.isDirty = false;
  }
  // mode: enum
  if (newViewProps.mode.isDirty) {
    swiftPart.setMode(static_cast<int>(newViewProps.mode.value));
    newViewProps.mode.isDirty = false;
  }
  // onBackgroundPress: function
  if (newViewProps.onBackgroundPress.isDirty) {
    swiftPart.setOnBackgroundPress(newViewProps.onBackgroundPress.value);
    newViewProps.onBackgroundPress.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return CherryStudioUI::CherryStudioUIAutolinking::isCherryBackgroundPressViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  CherryStudioUI::HybridCherryBackgroundPressViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  CherryStudioUI::HybridCherryBackgroundPressViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
