///
/// HybridCherryBackgroundPressViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridCherryBackgroundPressViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::cherrystudio::ui::views {

  extern const char HybridCherryBackgroundPressViewComponentName[] = "CherryBackgroundPressView";

  HybridCherryBackgroundPressViewProps::HybridCherryBackgroundPressViewProps(const react::PropsParserContext& context,
                                                                             const HybridCherryBackgroundPressViewProps& sourceProps,
                                                                             const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    enabled([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("enabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.enabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("CherryBackgroundPressView.enabled: ") + exc.what());
      }
    }()),
    mode([&]() -> CachedProp<BackgroundPressMode> {
      try {
        const react::RawValue* rawValue = rawProps.at("mode", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.mode;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<BackgroundPressMode>::fromRawValue(*runtime, value, sourceProps.mode);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("CherryBackgroundPressView.mode: ") + exc.what());
      }
    }()),
    onBackgroundPress([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onBackgroundPress", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onBackgroundPress;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onBackgroundPress);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("CherryBackgroundPressView.onBackgroundPress: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridCherryBackgroundPressViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridCherryBackgroundPressViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("CherryBackgroundPressView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridCherryBackgroundPressViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("enabled"): return true;
      case hashString("mode"): return true;
      case hashString("onBackgroundPress"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridCherryBackgroundPressViewComponentDescriptor::HybridCherryBackgroundPressViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridCherryBackgroundPressViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                                     const std::shared_ptr<const react::Props>& props,
                                                                                                     react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridCherryBackgroundPressViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridCherryBackgroundPressViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridCherryBackgroundPressViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridCherryBackgroundPressViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridCherryBackgroundPressViewProps>& props = std::const_pointer_cast<HybridCherryBackgroundPressViewProps>(constProps);
    HybridCherryBackgroundPressViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::cherrystudio::ui::views
