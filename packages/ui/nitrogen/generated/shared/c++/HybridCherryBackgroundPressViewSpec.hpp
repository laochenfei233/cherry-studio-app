///
/// HybridCherryBackgroundPressViewSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `BackgroundPressMode` to properly resolve imports.
namespace margelo::nitro::cherrystudio::ui { enum class BackgroundPressMode; }

#include "BackgroundPressMode.hpp"
#include <functional>

namespace margelo::nitro::cherrystudio::ui {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `CherryBackgroundPressView`
   * Inherit this class to create instances of `HybridCherryBackgroundPressViewSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridCherryBackgroundPressView: public HybridCherryBackgroundPressViewSpec {
   * public:
   *   HybridCherryBackgroundPressView(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridCherryBackgroundPressViewSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridCherryBackgroundPressViewSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridCherryBackgroundPressViewSpec() override = default;

    public:
      // Properties
      virtual bool getEnabled() = 0;
      virtual void setEnabled(bool enabled) = 0;
      virtual BackgroundPressMode getMode() = 0;
      virtual void setMode(BackgroundPressMode mode) = 0;
      virtual std::function<void()> getOnBackgroundPress() = 0;
      virtual void setOnBackgroundPress(const std::function<void()>& onBackgroundPress) = 0;

    public:
      // Methods
      

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "CherryBackgroundPressView";
  };

} // namespace margelo::nitro::cherrystudio::ui
