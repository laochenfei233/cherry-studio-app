# Plugin queries

Shared catalog and connection queries used by chat and plugin pages.
Connection subscriptions invalidate the shared query cache. Page navigation and post-connect refresh
remain owned by the plugin feature.
